/target
**/*.rs.bk
.env
# never commit cached tokens
token-cache.json
*.log
run-tunnel.sh
# the webhook binary only lands in deploy/ when assembling the release bundle
deploy/m365-webhook
target-musl/
# direnv cache (the .envrc itself is committed)
.direnv/

# demo data + mock Graph (local only, never committed)
demo/
